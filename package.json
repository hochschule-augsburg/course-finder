{
  "author": "",
  "description": "",
  "devDependencies": {
    "@types/node": "^20.12.11",
    "@types/node-schedule": "^2.1.7",
    "@vitejs/plugin-vue-jsx": "^3.1.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-perfectionist": "^2.10.0",
    "eslint-plugin-prettier": "^5.1.3",
    "eslint-plugin-sort-keys-fix": "^1.1.2",
    "eslint-plugin-typescript-sort-keys": "^3.2.0",
    "eslint-plugin-unused-imports": "^3.2.0",
    "eslint-plugin-vitest": "^0.4.1",
    "typescript-eslint": "^7.8.0",
    "vitest": "^1.5.3"
  },
  "keywords": [],
  "license": "MIT",
  "name": "subject-enroll",
  "scripts": {
    "dev": "pnpm --stream --parallel -r run dev",
    "build": "pnpm --stream --parallel -r run build",
    "test": "pnpm --stream --parallel -r run test",
    "lint": "pnpm --stream --parallel -r run lint",
    "lint:fix": "pnpm --stream --parallel -r run lint:fix",
    "stylelint": "pnpm --stream --parallel -r run stylelint",
    "stylelint:fix": "pnpm --stream --parallel -r run stylelint:fix"
  },
  "type": "module",
  "version": "0.0.0",
  "dependencies": {
    "@trpc/server": "^10.45.2",
    "node-schedule": "^2.1.1"
  }
}
