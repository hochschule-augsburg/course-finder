{
  "version": "0.2.0",

  "configurations": [
    {
      // does not seem to work
      "name": "api",
      "type": "node",
      "request": "launch",

      "program": "${workspaceFolder}/packages/api/src/main.ts",

      "runtimeExecutable": "${workspaceFolder}/packages/api/node_modules/.bin/tsx",

      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen",

      "skipFiles": [
        "<node_internals>/**",
        "${workspaceFolder}/packages/api/node_modules/**"
      ]
    },
    {
      // start pnpm dev:debug first
      "name": "Attach to process",
      "type": "node",
      "request": "attach",
      "port": 9229,
      "skipFiles": [
        "<node_internals>/**",
        "${workspaceFolder}/packages/api/node_modules/**"
      ]
    }
  ]
}
