/*
  Warnings:

  - You are about to drop the column `lastChange` on the `StudentChoice` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "StudentChoice" DROP COLUMN "lastChange";
